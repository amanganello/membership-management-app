# ---------------------------------------------------------------------------
# Copy this file to .env and adjust values. Do not commit .env.
# ---------------------------------------------------------------------------

# ----- Backend (required when running backend locally, e.g. Option B) -----
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/memberapp
PORT=3000
NODE_ENV=development

# ----- Optional: Docker Compose overrides ---------------------------------
# If you set these, docker compose will use them for the Postgres container.
# If you do not create .env, Postgres still runs with the defaults in docker-compose.yml.
# POSTGRES_DB=memberapp
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=postgres
